import { Callout } from 'nextra/components'

# Настройка на Linux

<Callout type="info" emoji="🐧">
  Существует множество клиентов для Linux. Мы рекомендуем **Nekoray** или **Hiddify**, так как они имеют графический интерфейс.
</Callout>

## 1. Скачивание клиента

### Nekoray (Рекомендуется)
Скачайте последнюю версию `.AppImage` или `.deb` файла с GitHub:

[Скачать Nekoray (GitHub)](https://github.com/MatsuriDayo/nekoray/releases)

### Hiddify
Альтернативный современный клиент:

[Скачать Hiddify (GitHub)](https://github.com/hiddify/hiddify-next/releases)

## 2. Установка (Ubuntu/Debian)

Если вы скачали `.deb` файл:

```bash
sudo dpkg -i nekoray-*.deb
sudo apt-get install -f # Если возникли ошибки зависимостей
```

Если вы скачали `.AppImage`:
1. Сделайте файл исполняемым:
   ```bash
   chmod +x Nekoray-*.AppImage
   ```
2. Запустите файл двойным кликом или через терминал.

## 3. Настройка

1. Запустите приложение.
2. Скопируйте вашу ссылку-подписку из [Telegram-бота](https://t.me/outlivion_bot).
3. В приложении:
   *   **Nekoray:** Нажмите `Program` -> `Add profile from clipboard`. Важно: Если спросит, выбирайте ядро **Sing-box**.
   *   **Hiddify:** Нажмите `+` или `New Profile` -> `Add from Clipboard`.
4. Нажмите кнопку подключения.
5. **Важно:** Чтобы VPN работал для всего компьютера, не забудьте поставить галочку **"System Proxy"** (или **"TUN Mode"** в настройках).
